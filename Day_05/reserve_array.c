#include <stdio.h>
int reverse_array(int arr[]) {
  t i;
  r(i = 4; i >= 0; i--) { tf("%d ", arr[i]); }
  // script test
  int main() {
    t arr[5] = {5, 4, 3, 2, 1};
    intf("Les éléments de tableau inversés: ");
    verse_array(arr);
    intf("\n");
    turn 0;
  }